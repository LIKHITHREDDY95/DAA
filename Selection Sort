SelectionSort(A):
    n = len(A)
    for i in range(0, n-1):
        min_index = i
        for j in range(i+1, n):
            if A[j] < A[min_index]:
                min_index = j
        swap A[i] with A[min_index]
